import { SearchLimitDirective } from './limit.directive';

describe('LimitDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchLimitDirective();
    expect(directive).toBeTruthy();
  });
});
